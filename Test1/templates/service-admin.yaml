apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nginxAdmin.name }}-svc
  namespace: {{ .Values.namespace.name }}
  labels:
    app: {{ .Values.nginxAdmin.name }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  selector:
    app: {{ .Values.nginxAdmin.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.nginxAdmin.port }}
  type: ClusterIP
